import { ok, err } from './result.js';
export function fromThrowable(fn, errorFn) {
    return (...args) => {
        try {
            return ok(fn(...args));
        }
        catch (e) {
            return err(errorFn ? errorFn(e) : e);
        }
    };
}
//# sourceMappingURL=from-throwable.js.map